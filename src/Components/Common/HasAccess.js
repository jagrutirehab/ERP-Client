import React from "react";
import PropTypes from "prop-types";

const HasAccess = ({ page, userAccess, children }) => {
  // page = { name: 'Patient', .... } for sub access = { name: 'Charting', ...permissions }
  // userAccess

  return <div></div>;
};

HasAccess.propTypes = {};

export default HasAccess;
