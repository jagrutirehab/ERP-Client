import React from "react";
import PropTypes from "prop-types";

const Medicine = (props) => {
  return (
    <React.Fragment>
      <div className="mt-3">Medicine Setting</div>
    </React.Fragment>
  );
};

Medicine.propTypes = {};

export default Medicine;
